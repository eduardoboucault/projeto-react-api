//* Para criar contexto global: Importar createContext, uma variável exportável e colocar como valor a chamada da função createContext.

import { createContext } from "react";

export const GlobalContext = createContext()